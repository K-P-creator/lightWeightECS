#pragma once

constexpr int WIDTH(1920);
constexpr int HEIGHT(1080);

unsigned int entityId = 0;

float paddleSpeed = 3.0f;
float startingBallSpeed = 2.0f;

//score tracking data
int leftScore = 0;
int rightScore = 0;